# 🎬 StreamAtlas

> A comprehensive digital entertainment management system built with C# and SQL.

**StreamAtlas** is an object-oriented application designed to manage a vast library of movies and streaming content. It demonstrates core software engineering principles including database normalization, class hierarchy, and efficient data retrieval.

---

## 🚀 Features

* **User Authentication:** Secure login and registration system.
* **Media Library:** Browse, search, and filter a catalog of movies and shows.
* **Database Integration:** Persistent data storage using **SQL** relational databases.
* **OOP Design:** Built using clean C# class structures (Inheritance, Polymorphism).
* **Watchlist Management:** Users can add or remove items from their personal list.

## 🛠️ Tech Stack

* **Language:** C# (.NET)
* **Database:** Microsoft SQL Server / T-SQL
* **Tools:** Visual Studio, Git

## 💻 Getting Started

To run this project locally:

1.  **Clone the repository**
    ```bash
    git clone https://github.com/bitcorrupt/StreamAtlas.git(https://github.com/bitcorrupt/StreamAtlas.git)
    ```
2.  **Setup Database**
    * Locate the `database_script.sql` file in the folder.
    * Run the script in SQL Server Management Studio (SSMS) to create the tables.
3.  **Run the App**
    * Open the solution file (`.sln`) in Visual Studio.
    * Press **F5** to build and run.

## 🔮 Future Roadmap

-  Add a Graphical User Interface (GUI)
-  Add admin panel for adding new movies

---
*Created by Kashaf, Zainab, Bisma*